import { registerRootComponent } from "expo";
import App from "./App";

registerRootComponent(App);
// export PATH="/usr/local/opt/openjdk@17/bin:$PATH" && export JAVA_HOME="/usr/local/opt/openjdk@17" && eas build --platform android --local

// installed jdk and android-commandlinetools for local builds
